// import { isShipNameSunken } from "../isShipNameSunken";
// import { isSunkenShip } from '../../isSunkenShip/isSunkenShip';

test("if all shipName coords are in gameBoardHitShots return true", () => {
  expect(true).toBe(true);
})