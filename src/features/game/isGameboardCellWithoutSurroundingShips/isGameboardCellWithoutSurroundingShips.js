export const isGameboardCellWithoutSurroundingShips = (array, index) => {
  // let copyArray = [...array];
  // if ((0 <= index <= 9)) {
  //   copyArray[index + 10] = "freemiss";
  // }
  // // if (())
  return (
    true
  )
}
